{
  "name": "arrowneon",
  "version": "0.1.0",
  "private": true,
  "description": "Prototype frontend cyberpunk NEON GRID pour Arrow — backend minimal de traitement des formulaires (contact / inscription).",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "CONTACT_DRY_RUN=1 node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "nodemailer": "^6.9.14"
  }
}
